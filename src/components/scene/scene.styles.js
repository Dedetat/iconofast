import { css } from 'glamor'

const container = css({
  margin: '0',
})

const responsive = css({
  position: 'absolute',
  height: '70%',
  display: 'flex',
  alignItems: 'flex-start',
})

export default {
  container,
  responsive,
}
