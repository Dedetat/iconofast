import { css } from 'glamor'

const container = css({
  margin: '0',
})

const responsive = css({
  height: '55%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  marginBottom: 'auto',
})

export default {
  container,
  responsive,
}
