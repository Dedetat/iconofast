import { css } from 'glamor'

const container = css({
  margin: '0',
})

const responsive = css({
  position: 'absolute',
  height: '60%',
  display: 'flex',
  marginTop: '10%',
  justifyContent: 'center',
  alignItems: 'center',
})

export default {
  container,
  responsive,
}
